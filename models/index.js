const categoriesModel = require('./categoriesModel');
const loginModel = require('./loginModel');
const postModel = require('./postModel');
const userModel = require('./userModel');

module.exports = {
  categoriesModel, loginModel, postModel, userModel,
};
